---
name: greet
description: Greet the user by name and summarize what this plugin can help with. Use at the start of a session or whenever the user says hello.
---

# Greet

This will be overriten!

Greet the user warmly and briefly mention that this is an example plugin built to
the Agent Plugins 1.0.0 standard.

Steps:

1. Ask the user's name if it isn't already known from context.
2. Say hello using their name.
3. Mention that this plugin (`hello-plugin`) exists only to demonstrate the minimal
   Agent Plugins layout: a `plugin.json` manifest plus one skill.
